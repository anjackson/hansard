<!DOCTYPE html>
%html{ :lang => 'en-GB' }
  %head
    %meta{ 'http-equiv'.to_sym=>"Content-Type", :content=>"text/html;charset=utf-8" }
    %title= "UK Parliament: Hansard: " << ( strip_tags(@title) || "Please give me a title" )
    = stylesheet_link_tag "screen", :media => "screen"
    = stylesheet_link_tag "print", :media => "print"
    = javascript_include_tag :defaults
  %body
    #header
      = day_nav_links
      = search_form
      %br{:clear => "all"}
    = section_nav_links(@section) if @section
    #content
      %h1
        = @title
      =@marker
      = yield
      #footer
        - if @day
          For this page:
          = link_to "XML source.", sitting_date_source_url(@sitting)
          = link_to "XML output.", sitting_date_xml_url(@sitting)