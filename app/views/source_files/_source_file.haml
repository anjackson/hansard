%div
  =link_to source_file.name, source_file_url(source_file)
  -if source_file.log
    %em
      =source_file.log_line_count
      -if source_file.log_line_count > 1
        problems
      -else
        problem
        