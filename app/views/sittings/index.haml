- @title = resolution_title(@sitting_type, @date, @resolution) unless @title

#timeline
  = sitting_timeline(@date, @timeline_resolution, @sitting_type)

= timeline_search_form(params) unless is_home?

