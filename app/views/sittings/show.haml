- @javascript = 'prototype'
- if @sitting
  = render :partial => 'partials/hansard_header'
  .debates
  - if @sitting.is_a? WrittenAnswersSitting
    =render :partial => "partials/section_link", :collection => @sitting.groups
  - else
    =render :partial => "partials/section_link", :collection => @sitting.debates.sections
- else
  .no-sittings
    No information was found for this period.